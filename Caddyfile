:3000 {
    # Serve the PWA
    root * /srv
    try_files {path} /index.html
    file_server

    # Serve transaction files with directory listing + CORS
    handle /data/* {
        root * /transactions
        file_server browse
        header Access-Control-Allow-Origin *
    }
}